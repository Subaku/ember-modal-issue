<LastSeenFilter
  @visionOptions={{visionOptions}}
  @visionValue={{visionValue}}
  @timeTypes={{timeTypes}}
  @timeValue={{timeValue}}
  @timeType={{timeType}}
  @onTimeValueChanged={{action timeValueChanged}}
  @onTimeTypeChanged={{action timeTypeChanged}}
  @onVisionChanged={{action visionValueChanged}}/>


<AssetLocationSummaries
  @summariesTask={{model.summariesTask}}
  @fetchAssetLocations={{action fetchAssetLocations}}/>
